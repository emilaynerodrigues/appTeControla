import React from 'react';
import { View, Text } from 'react-native';

export default function NewDespesaScreen({ navigation }) {
 return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>ADICIONAR NOVA DESPESA</Text>
  </View>

  );
}